"use strict";
document.documentElement.classList.add('js');
if('IntersectionObserver' in window){const observer=new IntersectionObserver(entries=>{for(const e of entries)if(e.isIntersecting){e.target.classList.add('visible');observer.unobserve(e.target);}},{threshold:.08});document.querySelectorAll('.reveal').forEach(e=>observer.observe(e));}else document.querySelectorAll('.reveal').forEach(e=>e.classList.add('visible'));
const steps=[['받은 자료는 수신함에서 검토합니다. 채택한 자료가 있으면 PC의 감독 루프가 절차 초안을 만드는 과제를 시작합니다.','01 / COLLECT','사용자가 선택한 자료만 수집하고, PC에서 검토 후 채택합니다.'],['무료 로컬 모델이 자료를 확인하는 절차 초안을 만듭니다. 새 과제가 없으면 대기하고 실패하면 간격을 두고 재시도합니다.','02 / DRAFT','현재 자동 작업: 채택 자료의 절차 초안 · 등록한 피어의 자료 확인.'],['초안의 유용성을 직접 확인하고 근거를 남깁니다. 검토한 절차만 선택해 공유하고, 다른 Cell도 자기 환경에서 다시 검토합니다.','03 / VERIFY','후보 생성은 지능 향상 증명이 아닙니다. 검증과 피드백으로 다음 방법을 개선합니다.']];
for(const b of document.querySelectorAll('[data-step]'))b.addEventListener('click',()=>{const i=Number(b.dataset.step);document.querySelectorAll('[data-step]').forEach(x=>x.setAttribute('aria-selected',String(x===b)));document.getElementById('flow-message').textContent=steps[i][0];const e=document.getElementById('flow-evidence');e.querySelector('span').textContent=steps[i][1];e.querySelector('p').textContent=steps[i][2];document.querySelectorAll('.flow-cell').forEach((x,j)=>x.classList.toggle('active',j<=i));});
for(const b of document.querySelectorAll('[data-copy]'))b.addEventListener('click',async()=>{try{await navigator.clipboard.writeText(document.getElementById(b.dataset.copy).textContent);b.textContent='복사했습니다';}catch{b.textContent='위 명령을 직접 복사해 주세요';}});
